name: expense_tracker
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: any
  hive: any
  hive_flutter: any
  uuid: ^4.4.2
  fl_chart: ^0.68.0
  timezone: any
  flutter_animate: ^4.5.0
  path_provider: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: any
  build_runner: any
  hive_generator: any
  hive_test: any
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/